import { LearningPathView } from "./_components/learning-path-view";

/**
 * /dashboard/learn/path — the self-assessing learning path.
 * Server Component shell; the interactive view is a client component.
 */
export default function LearningPathPage() {
  return <LearningPathView />;
}
